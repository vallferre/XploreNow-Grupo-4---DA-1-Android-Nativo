<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Estilo base heredado del tema de la app -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/explore_accent</item>
        <item name="colorPrimaryDark">@color/explore_accent_pressed</item>
        <item name="colorAccent">@color/explore_accent</item>
    </style>

    <!-- Cada campo individual del código OTP (ancho vía layout_weight en el contenedor) -->
    <style name="OtpField">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginStart">3dp</item>
        <item name="android:layout_marginEnd">3dp</item>
        <item name="android:background">@drawable/bg_otp_field</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#1A1A2E</item>
        <item name="android:textAlignment">center</item>
    </style>

</resources>
